{% extends 'admin/base_panel.html.twig' %}

{% block title %}{{ parent() }} - {% block section_title %}{% trans %} Add user {% endtrans %}{% endblock %}{% endblock %}

{% block page_content %}
    {% for msg in app.session.flashBag.get('error') %}
        <div class="alert alert-danger py-2" role="alert">
            {{ msg }}
        </div>
    {% endfor %}
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas {% block icon %}fa-user-plus{% endblock %}"></i>
            {% block card_title %}{% trans %} Add new user {% endtrans %}{% endblock %}
        </div>
        <div class="card-body">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
